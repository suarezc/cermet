# Pinned so every build — local, CI, contributor — compiles with the same stable.
# Components are declared here so a fresh rustup sync carries the fmt/clippy gates too.
[toolchain]
channel = "1.96.0"
components = ["rustfmt", "clippy"]
