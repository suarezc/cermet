# The local toolchain in this environment is rustc 1.49 (2020), which cannot
# build the secure stack (pyo3, aes-gcm, rusqlite all need ~1.63+).
# Run `rustup update stable` once; this pins the project to a modern stable.
[toolchain]
channel = "1.96.0"
