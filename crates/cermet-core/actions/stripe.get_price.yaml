# Retrieve one exact price's amount and recurrence summary without metadata or lookup keys.
provider: stripe
action: get_price
fields:
  - { name: price, type: str, required: true, class: identity, binding: exact_resource_pin }
consumes: [price]
execution_targets: [price]
http:
  steps:
    - id: get
      method: GET
      path: /v1/prices/{price}
      success_statuses: [200]
      require: [id, object, product]
